# Copied to ${project.basedir}/config/application.yaml at the pre-integration-test
# phase (see pom.xml) and removed again at post-integration-test — the same
# config/application.yaml override mechanism AI-workspace's real deployments
# use, exercised by GpuBrokerRealClusterIT against the real W206 cluster.
broker:
  nodes:
    - 192.168.5.0/26
  capabilities:
    "192.168.5.14:8000":
      max-concurrency: 1
